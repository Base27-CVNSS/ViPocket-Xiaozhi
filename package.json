{
  "name": "vipocket-xiaozhi",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "description": "Single-process Windows-portable Xiaozhi browser voice client and secure local gateway.",
  "scripts": {
    "start": "node apps/gateway/src/standalone.mjs",
    "dev": "node --watch apps/gateway/src/standalone.mjs",
    "test": "node --test apps/gateway/test/session-store.test.mjs",
    "check": "node --check apps/gateway/src/standalone.mjs && node --check apps/gateway/src/activation-service.mjs && node --check apps/web/src/main.js && node --check apps/web/src/audio/voice-engine.js && npm test"
  },
  "dependencies": {
    "ws": "8.18.2"
  },
  "engines": {
    "node": ">=20.11"
  }
}
